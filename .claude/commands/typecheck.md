Ejecutar verificación de tipos TypeScript: `npm run typecheck` en /root/bootcamp.

Reportar solo errores (no warnings). Si está limpio, confirmar "typecheck limpio ✓". Si hay errores, mostrar cada uno con archivo:línea y una sugerencia de fix de una línea.
